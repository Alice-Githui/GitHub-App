import { CountYearsPipe } from './count-years.pipe';

describe('CountYearsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountYearsPipe();
    expect(pipe).toBeTruthy();
  });
});
